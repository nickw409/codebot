version: 1.3
build:
  file: "ellipsis.Dockerfile"
commands:
  - command: "pytest eval/ -x"
    description: "Run the evaluation suite."
  - command: "ruff check ."
    description: "Lint the codebase."
